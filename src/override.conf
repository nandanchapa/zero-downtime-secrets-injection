# Path: /etc/systemd/system/application-server.service.d/override.conf

[Service]
# Clear the vendor default ExecStart entry point
ExecStart=
# Redirect systemd to initialize via the production environment injector wrapper
ExecStart=/usr/local/bin/production-env-injector.sh
